import { Layout } from 'antd'
import './header.css'

const { Header } = Layout

const _Header = () => {
  return <Header className="header">
    Сушеные ягоды и фрукты
  </Header>
}

export default _Header